# Scope Check Commands

```bash
# Changed files against PR base
git diff --name-only <base>...HEAD

# Optional: show names with status
git diff --name-status <base>...HEAD
```

Compare output against issue-approved `file touch scope`.
