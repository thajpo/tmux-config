interface:
  display_name: "Lean Flow"
  short_description: "Plan in current files and route to execution chain"
  default_prompt: "Use lean-flow to manage Brainstormed->Specd and implement a ready spec via internal chained skills."
